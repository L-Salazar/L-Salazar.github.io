name: salazar_site
description: Portfolio profissional em Flutter Web

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  url_launcher: ^6.2.2
  flutter_svg: ^2.0.9
  animated_text_kit: ^4.2.2
  visibility_detector: ^0.4.0+2
  font_awesome_flutter: ^10.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
